<%- model_class = UserDetail -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:user_id) %></th>
      <th><%= model_class.human_attribute_name(:name_ja) %></th>
      <th><%= model_class.human_attribute_name(:name_en) %></th>
      <th><%= model_class.human_attribute_name(:department_id) %></th>
      <th><%= model_class.human_attribute_name(:grade_id) %></th>
      <th><%= model_class.human_attribute_name(:tel) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @user_details.each do |user_detail| %>
      <tr>
        <td><%= link_to user_detail.id, user_detail_path(user_detail) %></td>
        <td><%= user_detail.user_id %></td>
        <td><%= user_detail.name_ja %></td>
        <td><%= user_detail.name_en %></td>
        <td><%= user_detail.department_id %></td>
        <td><%= user_detail.grade_id %></td>
        <td><%= user_detail.tel %></td>
        <td><%=l user_detail.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_user_detail_path(user_detail), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      user_detail_path(user_detail),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_user_detail_path,
            :class => 'btn btn-primary' %>
